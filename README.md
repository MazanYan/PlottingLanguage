# PlottingLanguage
Language for a pet project, is used to recognize console input and build a plot from it. Syntax and lexical analysis is generated with use of ANTLRv4.

## Usage
1. Create instance of LanguageRecognizer class providing input stream from which to read a file with code.
2. Boolean methods checkSyntax and writeToFile are used to self-explanatory things.
